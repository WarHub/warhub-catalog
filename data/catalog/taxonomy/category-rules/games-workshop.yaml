manufacturer: games-workshop
reason: >-
  GAMES WORKSHOP NUMBERS ITS ENTIRE RANGE, and the number says which game a product belongs to.
  An 11-digit GW code is `d12 d34 d56 d78 serial`: d12 is the language/regional pack (60 English,
  01 French, 15 Chinese, 99 language-neutral), d34 is the product type (12 plastic kit, 03 codex,
  10 Black Library paperback, 18 paint pot, 24 White Dwarf, 85/86 Forge World), d56 is the GAME
  SYSTEM and d78 is the FACTION. Decoded and measured 2026-08-31 over the committed catalog.


  d56 alone predicts gameSystem at 95.2% over 4,076 independently-classified products, against
  44.2% for d34 and 53.6% for d78 -- so the signal is specifically there and not a coincidence of
  correlated digits. The clauses below take it from 95.2% to 99.89% by naming the four families
  that account for every disagreement, and the veto block is where three of them live.


  WHY THIS TABLE IS ALLOWED TO EXIST AT ALL: none of it is inference about what GW meant. The
  segments are GW's own filing, recovered from GW's own identifiers, and every mapping below is
  checked against products whose gameSystem was stated INDEPENDENTLY of the code -- by
  legacy-catalog or the Algolia store feed. LLM-labelled products were excluded from that
  measurement entirely; 1,195 GW products carry a Haiku label and grading a code rule against a
  guess would prove nothing.


  SIX SEGMENTS BECAME REAL SLUGS. Necromunda (05), Warhammer Underworlds (07), Blood Bowl (09),
  Aeronautica Imperialis (18), Legions Imperialis (26) and Adeptus Titanicus (03) were all filed
  as `other-games` before, because the taxonomy had no word for them -- the catalog was less
  specific than the manufacturer. See taxonomy/game-systems.yaml.

# Vetoes first, and each names the AXIS it silences. Every one of these is a family where the
# code cannot settle WHICH GAME, and a rule that answered anyway would be confidently wrong rather
# than silent -- but the same products' CATEGORY is not in doubt at all, and until `blocks` existed
# these vetoes were suppressing that answer too, for 1,635 products.
#
# THEY ARE ABOUT UNCERTAINTY, NOT ABOUT DUAL MEMBERSHIP, and the difference decides whether a list
# is the fix. Earlier notes here read as though these classes were products belonging to two games
# at once, which `gameSystems` could now express. MEASURED 2026-09-01 against independently-stated
# systems, they are not: they are HETEROGENEOUS classes, each product belonging to exactly one game
# the code does not name. Forge World's 670 split horus-heresy 331 / other-games 169 /
# middle-earth 76 / warhammer-40k 47 / the-old-world 46 / age-of-sigmar 1 -- it is a production
# line that makes kits for every GW game, not a game. Writing two slugs into the list for these
# would assert a membership no source claims.
noneOf:
  # FORGE WORLD is a production line, not a game: d34 85/86 spans six systems (counts above).
  # Vetoing the GAME costs nothing -- all 670 already carry one, so no coverage is lost and 21
  # errors go -- while the category is 99.8% `miniatures` on 410 independently-graded products.
  - codeMatches: '^\d{2}8[56]\d{7}$'
    blocks: [gameSystems]
    note: Forge World spans six systems; the code says which line, not which game

  # BLACK LIBRARY: d78 81 is a novel, and d56 then carries the SETTING of the fiction rather than a
  # game the product belongs to. 430 of them sit under d56 01, where the split between Warhammer
  # 40,000 and Horus Heresy is the book's period and no field encodes it -- measured 86.3%, which
  # is not deterministic. The ones whose segment is unambiguous are recovered by a clause below.
  - codeMatches: '^\d{6}81\d{3}$'
    blocks: [gameSystems]
    note: Black Library, setting is not the game

  # SEGMENTS WHERE THE STATED EVIDENCE ITSELF SPLITS (Custodes, Knights, Titans, Chaos). These
  # are the same shape as Forge World and not dual membership either: `3008/3043` grades
  # horus-heresy 56 / warhammer-40k 21, `0108/0117` grades warhammer-40k 105 / horus-heresy 5 /
  # other-games 3. Refusing 266 products removes the last 17 disagreements and is what takes the
  # ladder to 99.89%.
  - codeMatches: '^\d{4}30(08|43)\d{3}$'
    blocks: [gameSystems]
    note: mixed Horus Heresy / Warhammer 40,000 segment, 56 vs 21
  - codeMatches: '^\d{2}12(01)99\d{3}$'
    blocks: [gameSystems]
    note: 40k plastic kits with a generic faction segment

  # TWO VETOES THAT STOOD HERE ARE GONE, AND THE PARAGRAPH ABOVE IS WHY THEY WERE WRONG. It says
  # these classes are heterogeneous rather than dual -- a real distinction, and the right call for
  # Forge World, whose 670 products split six ways. It is not the right call for these two, and
  # re-measuring them the way `gameSystems` being a LIST makes possible is what showed it.
  #
  # `0108/0117` (Adeptus Custodes / Imperial Knights, 302 products, 146 of them undecided). The
  # old note read "105 vs 5". Measured 2026-09-01 over every source EXCEPT the trade sheet this
  # table decodes: 92 products where the sources agree, and they agree on `warhammer-40k` 92 times
  # against `other-games` once. The disagreements are 10, and 7 of them are the same disagreement
  # -- `Custodian Guard`, `Prosecutor Squad`, `Custodian Wardens` carry BOTH `horus-heresy` and
  # `warhammer-40k`, because the Custodes plastics are sold into both games. That is not a segment
  # the code cannot resolve; it is a segment whose products belong to two games, which is the thing
  # a list can now say and a scalar could not.
  #
  # `1202(01|07|13)` (Age of Sigmar plastic kits, 360 products, 112 undecided). Same measurement:
  # 197 agree on `age-of-sigmar`, 3 on `other-games`, 2 on `warhammer-40k`, and 4 disagree --
  # `Skull Altar`, `Tzaangors`, `Spearhead: Blades of Khorne` carrying age-of-sigmar AND
  # warhammer-40k. 97.5% is not a segment that fails to name a game.
  #
  # NEITHER NEEDS A NEW CLAUSE. `^\d{4}01\d{5}$ -> warhammer-40k` and `^\d{4}02\d{5}$ ->
  # age-of-sigmar` already cover both blocks; the vetoes were the only thing stopping them. And
  # the manufacturer rung fires only on an empty field, so the 11 dual-membership products keep
  # what their sources say and the 258 that say nothing gain the segment's answer.

clauses:
  # =============================================================================================
  # WHAT THE THING IS. d34 -- digits 3-4 -- is GW's PRODUCT TYPE, and it answers the category
  # question as cleanly as d56 answers the game one. Measured 2026-09-01 against a control built
  # from two sources that never see this code: `ret-goblingaming`'s productType shelf through its
  # own committed table (the repo's only pure format taxonomy) and `mfr-gw-algolia`'s facet tree
  # (`> Unit Type` / `> Force Organisation` -> miniatures, `> Game Rules` -> book, and so on).
  # The two controls were checked against EACH OTHER first: 1,647 of 1,668 agree, 98.7%.
  #
  # `ret-gamenerdz` IS EXCLUDED FROM THE CONTROL, and the exclusion is load-bearing. Its one
  # surviving clause maps a department called `Paint and Crafting`, which holds brushes, tools,
  # bases and painting handles alongside paint. Including it drops `d34=19 -> hobby-auxiliary`
  # from 100% to 54% purely because that store calls a drybrush a paint.
  #
  # EVERY CLAUSE IN THIS FILE IS ANCHORED TO ELEVEN DIGITS, and the ones that were not were
  # actively wrong. `codeMatches` is an unanchored `re.search`, and 603 of the 9,271 GW codes are
  # not GW codes at all -- 345 six-digit house numbers from a German retailer import, 45 barcodes,
  # 201 non-numeric retailer catalogue numbers. The category clauses added on 2026-09-01 were
  # anchored from the start; THE GAME-SYSTEM SEGMENT CLAUSES BELOW WERE NOT, and they had been
  # publishing nonsense on 31 products. At least 16 are unarguable:
  #
  #     136614 White Dwarf 527                        -> middle-earth
  #     109102 Layer: Tau Light Ochre (12ml)          -> age-of-sigmar   (a pot of paint)
  #     134105 Lumineth Realm-Lords: Schriftrollenkarten -> necromunda
  #     135805 Warhammer 40.000: Intercessors + Paints  -> necromunda
  #     128127 Horus Heresy: Sammelband 1             -> the-old-world
  #     129507 Lazarus: Klinge Der Feindschaft (novel) -> warhammer-underworlds
  #     134109 First Marshal (Hb) (novel)             -> blood-bowl
  #
  # The unanchored `^\d{2}24 -> book` was also stamping `book` on `132489 Legiones Astartes: MkII
  # Veteran Squad`, a plastic kit. A six-digit shop number simply is not a GW product code, and a
  # regex that reads its first four characters as a language and a product type is inventing one.
  #
  # THE COST IS 44 PAINTS THAT MATCHED BY ACCIDENT. `^\d{2}(17|18)99` was reaching 33 thirteen-digit
  # barcodes whose leading digits happen to spell a paint segment, and `^\d{2}(19|21|22|23)99` 11
  # more. They were right by coincidence, which is not a reason to keep a rule that cannot tell why.
  # The `^(BASE|LAYER|SHADE|...)\s*:` name clause below reaches that population deliberately: 237
  # GW products, 231 of which already publish `paint`, with zero contradicted.
  #
  # WHY THE CODE BEATS THE NAME LEXICON HERE, and it is not a preference. The 2,667 products
  # mfr-gw-trade is the sole source for are overwhelmingly not in English:
  # `CARTES TECHNIQUES:`, `DATENBLATTKARTEN:`, `TOME DE BATAILLE:`, `SCHRIFTROLLENKARTEN:`,
  # `TARJETAS DATOS:`. The lexicon's patterns are English by construction; a digit pair is not.

  # NARROWEST FIRST. Bare bases sit inside the character and plastic-kit segments, and
  # categories.yaml is explicit that a base sold on its own is a component, not a model.
  - nameMatches: '\b(BASES?|STEMS?)\b.*\b(SET|PACK|KIT|DETAIL|\d+\s?MM)\b|\b\d+\s?MM\b.*\bBASES?\b|\b(HERO|INDUSTRIAL|VOID WAR)\s+BASES?\b'
    category: hobby-auxiliary
    note: >-
      17 products; control 46, 97.8%. Widened from the original 13 to catch `LARGE BASE DETAIL
      KIT`, `INDUSTRIAL BASES`, `WH40K HERO BASES` and `VOID WAR BASES` -- and it must stay AHEAD
      of the SECTOR IMPERIALIS terrain clause below, which sells scenery and base kits off the
      same shelf name.

  # =============================================================================================
  # TERRAIN, BY NAME, AND WHY IT HAS TO BE BY NAME. Terrain appears NOWHERE in GW's product code:
  # a `Skull Altar` and a `Putrid Blightkings` box share d34 12 and sit in the same stock section.
  # It is the one category the numbering cannot express, and it is why `d34=12` could not simply
  # be read as `miniatures` until these clauses existed.
  #
  # THE RANGE NAMES ARE THE SIGNAL. GW sells its generic scenery under a handful of branded
  # ranges, and those names are stable and specific. What this CANNOT reach is faction terrain
  # named like a unit -- `Awakened Wyldwood`, `Skaven Gnawholes`, `Feculent Gnarlmaw`,
  # `Bad Moon Loonshrine` -- and a name net over the known-terrain population recalls only 49% for
  # exactly that reason. Those are named products, not a class, and `overrides.yaml` is their
  # honest home.
  #
  # A NOTE ON THE CONTROLS, because they disagree with each other here. `ret-goblingaming` shelves
  # `Bone-tithe Nexus`, `Realmshaper Engine`, `Bossrokk Tower` and `Hammerfall Bunker` under
  # Miniatures while `mfr-gw-algolia` shelves them under Scenery. Part of GW's terrain/miniature
  # boundary is genuinely contested by the people selling it, and no rule resolves that.
  - nameMatches: '\bBATTLEZONE\b|\bB/Z(?:ONE)?\b'
    category: terrain
    note: 10 undecided; control 9/9
  - nameMatches: '\bZONE\s+MORTALIS\b|\bTHATOS\s+PATTERN\b'
    category: terrain
    note: 6 undecided; control 11/13 -- the 2 misses are a Necromunda rulebook and a base set
  - nameMatches: '\bDOMINION\s+(OF\s+)?SIGMAR\b|\bAZYRITE\b|\bREALMSCAPE\b|\bREALM\s+OF\s+BATTLE\b|\bRAVAGED\s+LANDS\b|\bETHERIC\s+VORTEX\b'
    category: terrain
    note: 9 undecided; thin control (1/1) but all 19 hits read terrain on a full name read
  - nameMatches: '\b(ROHAN|GONDOR|DOL\s+GULDUR|LAKE-?TOWN|MINAS\s+TIRITH)\b.*\b(HOUSE|TOWER|WATCHTOWER|STRONGHOLD|MANSION|RUINS|PALISADE|FORTRESS)\b'
    category: terrain
    note: 8 undecided; control 7/7
  - nameMatches: '\b(RUINS|RUINED)\b'
    category: terrain
    note: >-
      8 undecided; control 10/10 over 21 hits. The SINGULAR `RUIN` is deliberately excluded --
      `Varanguard Knights of Ruin` is a cavalry unit.
  - nameMatches: '\bSECTOR\s+(IMPERIALIS|MECHANICUS)\b|\bS/MECHANICUS\b|\bS/IMPERIALIS\b'
    category: terrain
    note: 10 undecided; the BASES clause above takes the base kits off this shelf first
  - nameMatches: '\bKILL\s?ZONE\s*:'
    category: terrain
    note: >-
      7 undecided. The COLON is load-bearing: it selects the `Killzone: Gallowdark` terrain sets
      and excludes `Killzone Upgrade: ...` and `Kill Team: Killzone Essentials`, which are not.

  - nameMatches: '\bGAMING PACK\b'
    category: game-accessory
    note: 11 undecided; control 2/2

  # THE THREE THINGS THE d34 22 BLOCK CANNOT SAY FOR ITSELF, and they are named products rather
  # than a class. `03` is excluded from that clause because the segment is mostly Adeptus
  # Titanicus WEAPON SETS (control 4 miniatures against 2 game-accessory), and `18` because of
  # Aeronautica base packs -- but each holds a small run of cards or boards that is unambiguous by
  # name. Reaching them here costs the segments nothing.
  - nameMatches: '\bSTRATAGEM CARDS?\b|\bCOMMAND TERMINAL\b|\bAREA OF ENGAGEMENT\b|\bA\.O\.E\b'
    category: game-accessory
    note: 6 undecided; all Adeptus Titanicus card packs and Aeronautica board sets

  # A BASING SET IS NOT A BUNDLE OF MODELS, and this runs ahead of the `9902` clause below for
  # exactly one product: `Athel Loren Basing Set` is the single name in GW's whole web-bundle
  # segment that the segment gets wrong.
  - nameMatches: '\bBASING SET\b'
    category: hobby-auxiliary
    note: 1 undecided; categories.yaml puts basing materials with the components

  # THE PAINT RANGE NAMES, which is how a Citadel colour is recognised now that the paint segment
  # clause is anchored to eleven digits and no longer matches a barcode by accident. 237 GW
  # products, 231 of them already publishing `paint` from other evidence and NONE contradicted.
  - nameMatches: '^\s*(BASE|LAYER|SHADE|DRY|AIR|CONTRAST|TECHNICAL|SPRAY|GLAZE|EDGE)\s*:'
    category: paint
    note: 6 undecided; 231/231 agreement with what other evidence already decided

  # THINGS PLAYED WITH. d34 05 is the card product type -- `DATACARDS:`, `INDEX CARDS:`,
  # `WARSCROLL CARDS:`, `PACK DE FACTION:`. 99.0% over a control of 102; both disagreements are
  # the control's, a datacard deck one store shelved `board-game` and a warscroll deck another
  # shelved `miniatures`.
  - codeMatches: '^\d{2}05\d{7}$'
    category: game-accessory
    note: 560 products; control 102, 99.0%
  # 70 IS THE OBJECTIVE-SET TYPE -- `OBJECTIVE SET: PARIAH NEXUS`, `CHAPTER APPROVED: OBJECTIVES
  # SET 2025-26`. Nine products, one of which another source already calls `game-accessory`, and
  # `category-lexicon.yaml` already reaches the plural form by name.
  - codeMatches: '^\d{2}70\d{7}$'
    category: game-accessory
    note: 2 undecided; control 1, 100%

  # d34 22 is dice, tokens, movement trays and objective markers -- EXCEPT where d56 says
  # otherwise: 99 is bases and water pots (already `hobby-auxiliary` by the clause below), 03 Titan
  # weapon sets, 18 and 26 base packs. Raw purity is 50%; with those four excluded it is 50 of 52.
  #
  # `09` WAS ON THAT LIST AND SHOULD NOT HAVE BEEN. The note said "09 Blood Bowl pitches", which is
  # what the segment holds -- and a Blood Bowl pitch is a printed play surface, which is what
  # `game-accessory` is. Measured 2026-09-01 over the whole segment: 49 products, control 28, of
  # which 26 say `game-accessory` and 2 say `terrain`. The two dissenters are pitches shelved as
  # scenery by one store, against 26 that are not, and the 21 products the exclusion was leaving
  # undecided are `BLOOD BOWL: NURGLE TEAM PITCH & DUGOUTS`, `BB HEAD COACH'S RULES &
  # ACCESSORIES PACK`, `BLOOD BOWL: NURGLE TEAM CARD PACK` -- a pitch, a rules-and-tokens pack and
  # a card deck.
  - codeMatches: '^\d{2}22(?!99|03|18|26)\d{7}$'
    category: game-accessory
    note: 87 products; control 52, 96.2%

  # THINGS READ. Three separate book types, measured separately because they are separate ranges:
  # 04 hardbacks and campaign supplements, 10 Black Library paperbacks, 03 codexes and battletomes.
  - codeMatches: '^\d{2}04\d{7}$'
    category: book
    note: 543 products; control 109, 98.2% -- both misses are Adeptus Titanicus supplements
  - codeMatches: '^\d{2}10\d{7}$'
    category: book
    note: 337 products; control 100, 99.0%
  - codeMatches: '^\d{2}03\d{7}$'
    category: book
    note: 193 products; control 54, 100%

  # THINGS USED TO BUILD AND FINISH. 51 is the transfer-sheet type (its Legion subset also decides
  # a game system, further down); 19/21/23 are brushes, glue and tools. The existing paint clauses
  # below reach these same types where d56 is 99; these two reach the rest.
  - codeMatches: '^\d{2}51\d{7}$'
    category: hobby-auxiliary
    note: 8 products; control 29, 100%
  - codeMatches: '^\d{2}(19|21|23)\d{7}$'
    category: hobby-auxiliary
    note: 4 products; control 25, 100%

  # MODELS. 85/86 is Forge World -- vetoed above for the GAME, because the line supplies six of
  # them, and 99.8% `miniatures` on a control of 410 regardless. That veto used to suppress this
  # answer too; `blocks: [gameSystems]` is what lets both be true.
  - codeMatches: '^\d{2}8[56]\d{7}$'
    category: miniatures
    note: 674 products; control 410, 99.8% -- the single miss is one terrain piece
  - codeMatches: '^\d{2}01\d{7}$'
    category: miniatures
    note: 323 army and starter boxes; control 21, 100%
  - codeMatches: '^\d{2}07\d{7}$'
    category: miniatures
    note: 118 character and upgrade kits; control 282, 100% once the BASES clause has run
  - codeMatches: '^\d{2}(06|11|80|81)\d{7}$'
    category: miniatures
    note: 167 single-model kits; control 457, 99.8%
  # 55 IS A FORGE WORLD WEAPON-OPTION TYPE and 14 a single Orks kit -- three products between them,
  # no control, and the names are unambiguous: `Legio Custodes Telemon Arachnus Storm Cannon`,
  # `Telemon Heavy Dreadnought Iliastus Accelerator Culverin`, `Badrukk's Flash Gitz`. Named here
  # rather than in overrides.yaml because they are a segment, not a correction.
  - codeMatches: '^\d{2}(14|55)\d{7}$'
    category: miniatures
    note: 3 products; no control

  # GW'S WEB-BUNDLE SEGMENT SPLITS ON ITS FIRST TWO DIGITS, which the `d34 02` packaging clause
  # below deliberately did not use: it says only `packaging: bundle` because "a bundle of codexes
  # and a bundle of kits are not the same thing, and the 51 products divide between them with no
  # signal to say which". There is a signal, and it is in the code. `99` is the model prefix and
  # `60` the publications one, exactly as everywhere else in GW's numbering: all 37 `9902` names
  # are army formations (`1st Company Task Force`, `Anti-air Defence Force`, `Adeptus Mechanicus
  # Army`, `Arjac's Shieldbrothers`), while all 14 `6002` names are collections of books and
  # starter material (`Adeptus Mechanicus Codex Collection`, `Warhammer Age of Sigmar Rules
  # (English PDF)`).
  #
  # d78 99 IS EXCLUDED and that is where the mixing actually lives: the generic-faction bundles are
  # `Warhammer 40,000 Battle Mat & Scenery Collection` (game-accessory), `Void Field Generatorum`
  # (terrain), `How to Paint Citadel Miniatures: Tau Empire (EPUB)` (book), `Artificer Brush
  # Collection` (hobby-auxiliary) and two paint bundles. Excluding it costs 2 real model bundles
  # (`Aeldari Bladehost`, `Athelorn Avengers`) and buys 11 products this clause would be wrong
  # about. The `60` half is left undecided: 14 products, and `Astra Militarum Starter Collection`
  # is models while `Adeptus Mechanicus Codex Collection` is books, on the same four digits.
  - codeMatches: '^9902\d{2}(?!99)\d{5}$'
    category: miniatures
    note: >-
      24 web bundles of models; control 0 -- nothing in this catalog states `miniatures` for a GW
      product, so this rests on a full name read of all 37 rows in the `9902` block

  # d34 12, THE PLASTIC-KIT TYPE, AND THE LAST CATEGORY CLAUSE IN THE FILE. It was refused once,
  # at 95.5% against a control of 3,028, because its 4.5% residue is terrain the code cannot
  # separate. What changed is not the bar but the clauses ahead of it: with the terrain names and
  # `GAMING PACK` taken out first, a full name read of ALL 1,076 remaining products puts the error
  # at 18 -- 1.7% -- which is inside the band `d34=04 -> book` already runs at.
  #
  # THE EVIDENCE IS A NAME READ, NOT A CONTROL, and that is a weaker instrument said plainly.
  # Nothing in this catalog independently states `miniatures` for a Games Workshop product, so
  # there is nothing to grade against; what stands behind this clause is that every one of those
  # 1,076 names was read. The block is 1,058/1,134 language-neutral `99...` codes, so they are
  # English -- the multilingual argument that justifies the code over the name lexicon elsewhere
  # in this file does NOT apply to this clause.
  #
  # THE 18 THAT REMAIN WRONG ARE NAMED PRODUCTS, NOT A CLASS: 10 terrain (`Noctilith Crown`,
  # `Battle Sanctum`, `Aegis Defence Line`, `Herdstone`, `Balewind Vortex`, `Feculent Gnarlmaw`,
  # `Fortress of Dol Guldur`, `Arcane Ruins`, `Tidewall Shieldline`, `Promethium Tanks`), 3
  # transfer/base sheets, 1 eBook, 1 Underworlds lair, and 3 shop-floor dispensers that are not
  # products at all. A rule cannot reach them; `overrides.yaml` can, one line each, and that is
  # the honest home for a product-specific correction.
  - codeMatches: '^\d{2}12\d{7}$'
    category: miniatures
    note: 1,067 undecided after the carve-outs; 18 known wrong on a full name read of all 1,076

  # d34 17 IS THE PAINT-BUNDLE TYPE, and its 73 products are the same handful of boxes repeated
  # once per language market -- `AOS SKAVEN + PAINT SET` exists under d12 52, 53, 54, 55, 57 and
  # 99. The names are `<faction> + PAINTS`, `<faction> PAINT SET+` and `PAINTS + TOOLS`.
  #
  # d56 99 IS EXCLUDED BECAUSE IT IS ALREADY DECIDED, further down: `^\d{2}(17|18)99\d{5}$` reaches
  # the seven `CITADEL COLOUR: <range> PAINT SET` boxes and calls them `paint`. That clause sits
  # after this one in the file and first-match-wins would otherwise hand them a different answer.
  # Whether `paint` is right for a seven-pot assortment is a real question and this is not the
  # change that should settle it.
  #
  # `paint-set` IS WHAT THE EVIDENCE SAYS, and the tension is worth naming: several of these boxes
  # contain a MODEL as well as paints, and categories.yaml defines `paint-set` as "a boxed
  # assortment of DIFFERENT paints sold as one unit". Seven of the eight independently-decided rows
  # in the segment say `paint-set` anyway (`Warhammer 40000 Orks + Paints`, `AOS S/E + PAINT SET`,
  # `TYRANID PAINT SET+`); the eighth is `Warhammer 40000 Paints + Tools` shelved
  # `hobby-auxiliary` by one store while another shelves the identical `AOS PAINTS+TOOLS` as
  # `paint-set`. That is the two stores disagreeing, not a signal.
  - nameMatches: '^\s*GETTING STARTED WITH\b'
    category: miniatures
    note: >-
      2 undecided (the Chinese-market rows); control 2, 100% -- the English rows of the same two
      products. GW's `Getting Started with...` is a magazine sold with a model, and both stores
      that filed it called it the model.
  - codeMatches: '^\d{2}17(?!99)\d{7}$'
    category: paint-set
    packaging: set
    note: 54 products; control 8, 87.5%

  # ---------------------------------------------------------------------------------------------
  # HOW MANY, AND ARE THEY THE SAME THING. `packaging` is additive-only, so these fill a form the
  # record does not have and never contradict a source.
  #
  # GW's trade sheets are the dense population the vocabulary predicted: 912 products name a
  # multiple in their own title, 887 of them paint. The `X1` guard is not decorative --
  # `SPACE MARINE HEROES BLISTER PACK (X1)` is a single.
  - nameMatches: '\((?!1[- ])\d{1,3}[- ]PACK\)|\bX\s?([2-9]|\d{2,3})\b'
    packaging: multipack
    note: 912 products, 875 of them a case of six identical pots

  # d34 02 is GW's WEB BUNDLE section, and it is the cleanest packaging signal in the whole code:
  # `Angel's Wrath Intervention Force Web Bundle`, `Adeptus Mechanicus Codex Collection`,
  # `2 x Imperial Knights: Renegade`. categories.yaml's test for `bundle` -- the components are
  # themselves products a shop sells alone -- holds for every name in it. The CATEGORY is
  # deliberately absent: a bundle of codexes and a bundle of kits are not the same thing, and the
  # 51 products divide between them with no signal to say which.
  - codeMatches: '^\d{2}02\d{7}$'
    packaging: bundle
    note: 51 web bundles; the only population `bundle` has beyond legacy's 19

  - nameMatches: '\b(EBOOK|EPUB|iBOOK|INTERACTIVE .{0,20}EDITION)\b|\(PDF\)'
    packaging: digital
    note: 8 products

  # =============================================================================================
  # WHICH GAME.
  # KILL TEAM has no segment of its own -- it lives inside 01 and 02 -- so it is claimed by name,
  # BEFORE the segment clauses. Until `kill-team` existed this had to be a veto, because the only
  # honest options were "call it 40k" (wrong for the ones GW files under other-games) or "skip it".
  # A slug turns 189 skipped products into decided ones. `K/T` and `WHU`-style contractions are
  # GW's own abbreviations on trade rows.
  - nameMatches: '\bKILL[ -]?TEAM\b|\bKILLZONE\b|\bK/T\b|\bK/TEAM\b'
    gameSystems: [kill-team]

  # A LEGION TRANSFER SHEET is Horus Heresy even though it sits in the 40k segment: d34 51 is the
  # transfer-sheet product type and the Legion sheets are period-specific.
  - codeMatches: '^\d{2}5101\d{5}$'
    gameSystems: [horus-heresy]

  # d56 == 99 IS NOT A GAME SEGMENT. It is what GW uses for things that belong to no game: paints
  # (d34 18), paint sets (17), brushes (19), sprays (20), glue (21), bases and dice (22), tools and
  # cases (23). Measured: 1,193 products, 1,149 of them unclassified. The 44 that carried a value
  # were all LLM guesses and all wrong -- `CONTRAST: ASURMEN BLUE` had even acquired the faction
  # `aeldari` from the colour's name. These decide a CATEGORY and deliberately no game system.
  - codeMatches: '^\d{2}(17|18)99\d{5}$'
    category: paint
  - codeMatches: '^\d{2}2099\d{5}$'
    category: paint
  - codeMatches: '^\d{2}(19|21|22|23)99\d{5}$'
    category: hobby-auxiliary
  # WHITE DWARF is a magazine, in every segment. 131 products, every name `WHITE DWARF <n> (...)`.
  - codeMatches: '^\d{2}24\d{7}$'
    category: book

  # AUDIO IS STILL A BOOK. d34 68 is Black Library's audio line -- 21 products, every one an
  # audiobook or audio drama (`HORUS HERESY: NIGHTFANE (AUDIOBOOK)`, `GOTREK: REALMSLAYER`,
  # `Asurmen: The Darker Road (CD)`). No control exists; all 21 names were read.
  #
  # `categories.yaml` says `book` is "anything whose content is TEXT", which reads against this at
  # first -- and its own boundary settles it the other way: "Digital editions are `book` with
  # `packaging: digital`, NOT a separate category: an eBook codex is the same thing as the printed
  # codex, differently delivered." That is describing the KIND OF WORK, not the substrate. An
  # audiobook of `Nightfane` is the same work as the novel `Nightfane`. A separate `audiobook`
  # value would repeat the axis collapse `paint-set` is the scar of, mixing what a thing IS with
  # how it arrives, for a population thinner than `event`.
  #
  # `packaging` is deliberately left ABSENT rather than set to `digital`: 3 of the 21 are `(CD)`,
  # which is a physical package, and GW's code does not say which are which. Minting an `audio`
  # packaging value for 21 products is a vocabulary decision the evidence does not force.
  - codeMatches: '^\d{2}68\d{7}$'
    category: book
    note: 21 products, all Black Library audio; no control, full name read

  - codeMatches: '^\d{2}70\d{2}81\d{3}$'
    category: merch
    note: >-
      6 products -- 2 poster maps, 3 bookmarks, `BLACK LIBRARY HORUS HERESY BOOK ENDS`. No
      control; full name read. `categories.yaml` names posters and art prints under `merch`, and
      a bookmark is not hobby equipment.

  - codeMatches: '^\d{2}63\d{7}$'
    category: book
    note: >-
      4 digital editions (`Apocalypse (Interactive iBook Edition)`, `Everchosen Painting Guide
      (ePub)`). They already carry `packaging: digital` from the EBOOK/EPUB name clause; only the
      category was missing.

  # The segments themselves. Every one of these is 100% pure against independently-stated evidence
  # once the vetoes above have run; the two that are not (01 at 92.4% and 30 at 94.0% raw) are
  # carried there by the veto block rather than by a lower bar.
  - codeMatches: '^\d{4}01\d{5}$'
    gameSystems: [warhammer-40k]
  - codeMatches: '^\d{4}02\d{5}$'
    gameSystems: [age-of-sigmar]
  - codeMatches: '^\d{4}03\d{5}$'
    gameSystems: [adeptus-titanicus]
  - codeMatches: '^\d{4}05\d{5}$'
    gameSystems: [necromunda]
  - codeMatches: '^\d{4}07\d{5}$'
    gameSystems: [warhammer-underworlds]
  - codeMatches: '^\d{4}09\d{5}$'
    gameSystems: [blood-bowl]
  - codeMatches: '^\d{4}14\d{5}$'
    gameSystems: [middle-earth]
  - codeMatches: '^\d{4}18\d{5}$'
    gameSystems: [aeronautica-imperialis]
  - codeMatches: '^\d{4}26\d{5}$'
    gameSystems: [legions-imperialis]
  - codeMatches: '^\d{4}27\d{5}$'
    gameSystems: [the-old-world]
  - codeMatches: '^\d{4}30\d{5}$'
    gameSystems: [horus-heresy]

  # d56 06 IS A SEGMENT THIS TABLE NEVER MAPPED: 24 products, 18 with no game system at all --
  # Blackstone Fortress (12), Space Marine Adventures, `Assassinorum: Execution Force`,
  # `Warhammer Quest: Blackstone Fortress`. `data/catalog/mappings/mfr-gw-trade.yaml` already
  # reads GW's own `WH Quest` range as `other-games`, and `other-games` is `catchAll: true`, so a
  # later rule that can name the game refines this rather than fighting it. The Kill Team products
  # in the segment keep `kill-team`: the name clause at the top of this file runs first.
  - codeMatches: '^\d{4}06\d{5}$'
    gameSystems: [other-games]
    note: 18 undecided; no control, full name read of all 24
